# Gemini CSKH Telegram Bot

## Cách chạy:
1. Tạo file `.env` từ `.env.example`, thêm key.
2. Tạo venv:
   ```
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   python main.py
   ```
